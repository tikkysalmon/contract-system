# ระบบทำสัญญาออนไลน์ — สถานะปัจจุบัน

แผนเต็มอยู่ที่ `C:\Users\user\.claude\plans\https-salmonenterprise-sg-larksuite-com-cozy-rose.md`

**⚠️ 2026-09-04: ย้ายขึ้นระบบจริงแล้ว** — repo จริงที่ deploy อยู่คือ `C:\Users\user\contract-system`
(GitHub: `tikkysalmon/contract-system`, public) ไม่ใช่โฟลเดอร์นี้อีกต่อไป โฟลเดอร์
`15_ระบบทำสัญญา\webapp` นี้เป็นสำเนาต้นทางที่ยังใช้พัฒนา/ทดสอบในเครื่องได้เหมือนเดิม แต่**ทุกครั้งที่แก้โค้ด
ต้อง copy ไฟล์ที่เปลี่ยนไปที่ `contract-system` แล้ว commit/push ด้วย ถึงจะขึ้นเว็บจริง** (ยังไม่ได้ตั้ง
auto-sync ระหว่าง 2 โฟลเดอร์)

## เสร็จแล้ว
- `public/sign.html` + `public/sign.js` + `public/validation.js` + `public/style.css` — ฟอร์มลูกค้าหลายขั้นตอน
  พร้อม validation (เลขบัตร ปชช. checksum, เบอร์โทร, กฎอายุ 19 ปี+อนุโลม 3 เดือน), ขั้นตอนผู้ปกครอง/ผู้ค้ำแบบมีเงื่อนไข,
  อัปโหลดรูปเอกสาร, ตารางผ่อนชำระ, ลายเซ็นวาดบนจอ + แนบรูปลายเซ็นผู้ค้ำ/ผู้ปกครองพร้อมเครื่องมือลบพื้นหลังสีอ่อน
  ใช้ `window.MOCK_SESSION` (ใน `mock-session.js`) แทนข้อมูลจริงจาก CRM ไปก่อน — เปิด `sign.html` ตรงๆ ในเบราว์เซอร์ทดสอบได้เลย
- `public/lib/`, `public/pdf-tools.js` — คัดลอกมาจาก `12_esign-approval` (pdf-lib + fontkit + ฟอนต์ไทย, พร้อมใช้ stamp ลายเซ็นลง PDF)
- `supabase-setup.sql` — schema เริ่มต้น (staff_users, contract_sessions, contract_submissions, storage bucket)
- `api/crm-lookup.js` — โครง Vercel function พร้อม mock data, รอ credentials จริง

## เสร็จเพิ่ม (2026-09-03 รอบ 2)
- **`api/crm-lookup.js` ใช้งานได้จริงแล้ว** — ยืนยันด้วยการเรียก CRM จริงสำเร็จ (ดู `CRM-API-NOTES.md`)
  ดึง `installmentType` มา map เป็น plan_type ได้ตรงตามที่ user ยืนยัน (ซื้อสด/วางดาวน์/ผ่อนไม่มีดาวน์)
  และดึง `dateOfBirth` จาก `/crm/customer/{id}` มาพรีฟิลให้ลูกค้าแทนที่จะให้พิมพ์เองได้ด้วย
  **ยังต้องตั้งค่า `CRM_USERNAME`/`CRM_PASSWORD` ใน Vercel project settings ก่อนขึ้นจริง** (ห้ามใส่ในไฟล์)

## เสร็จเพิ่ม (2026-09-03 รอบ 3)
- **`public/cs-review.html` + `cs-review.js`** — หน้า CS ค้นหาเลข SO จริง เห็นข้อมูลจาก CRM (ราคา/ส่วนลด/
  ยอดวางดาวน์หรือผ่อนสะสม/คงเหลือ/ชื่อ-วันเกิด-เบอร์ลูกค้า) กรอกยืนยันจำนวนงวด+วันเริ่มผ่อนงวดแรก
  (ตามที่ user ยืนยันว่าต้องให้ CS ใส่เองก่อนเสมอ ไม่ใช้ค่า CRM ตรงๆ) แล้วกด "สร้างลิงก์" ได้
- **`api/crm-lookup.js` map ยอดดาวน์/ยอดผ่อนสะสมถูกต้องแล้ว** — ยืนยันจากสกรีนช็อตจริงของ user:
  ใช้ `accumulatedAmount` (ไม่ใช่ `initAmount` ที่เข้าใจผิดไว้ก่อนหน้า) ตรวจสอบตัวเลขตรงกับที่ CRM แสดงเป๊ะ
  (productPrice − accumulatedAmount = คงเหลือ, คงเหลือ/จำนวนงวด = ยอดผ่อนต่องวด ตรงกับ "ขั้นต่ำ" ที่ CRM โชว์)
- **`public/sign.js`** เพิ่มขั้นตอนแรกสุด "รายการที่ทำสัญญา" แสดงครบ 9 รายการตามที่ user ระบุ ก่อนเข้าสู่ขั้นกรอกข้อมูลส่วนตัว
- **`dev-server.js`** — เซิร์ฟเวอร์ทดสอบในเครื่อง (ไม่ใช้ตอน deploy จริง) รัน `api/*.js` ได้จริงเพื่อทดสอบทั้ง flow
  ทดสอบแล้วจริงจบ end-to-end: CS ค้นหา SO จริง → เห็นข้อมูลจริงจาก CRM → สร้างลิงก์ → เปิดฟอร์มลูกค้าเห็นข้อมูลที่ CS กรอกจริง
  วิธีรัน: `CRM_USERNAME=... CRM_PASSWORD=... node dev-server.js` แล้วเปิด `http://localhost:3210/cs-review.html`

## เสร็จเพิ่ม (2026-09-03 รอบ 4) — เทมเพลตสัญญารวม 6 combo สำเร็จแล้ว
- **`templates/master-installment.docx` + `templates/master-downpayment.docx`** — เทมเพลตหลัก 2 ไฟล์ (แยกตามแผนผ่อน)
  แต่ละไฟล์รองรับทั้ง 3 กลุ่มลูกค้า (ทั่วไป/ผู้ค้ำ/ผู้ปกครอง) ผ่าน docxtemplater conditional tag
  `{#hasGuarantor}`/`{#hasGuardian}` — ครบทั้ง 6 combo แล้ว (เดิมมีแค่ 3 combo จาก 3 ไฟล์ต้นฉบับ)
  **ไม่มีการแก้ไขถ้อยคำใดๆ เลย** — ทุกย่อหน้า copy มาจากไฟล์ต้นฉบับ 3 ไฟล์แบบคำต่อคำ (ใช้ script ที่
  แทรกแค่ paragraph marker ควบคุมเงื่อนไข ไม่แตะเนื้อหาเดิม)
- **วิธีตรวจสอบที่ใช้ (`templates/verify_masters.js`, รันแล้ว PASS ทั้ง 14 จุดตรวจ):**
  1. Render เทมเพลตทดสอบด้วยข้อมูลปลอมผ่าน docxtemplater จริง (ไม่ใช่แค่ตรวจ syntax XML)
  2. เทียบ byte-ต่อ-byte กับผลลัพธ์จากไฟล์ต้นฉบับจริง (docx1/2/3) ในทุก combo ที่มีของเดิมให้เทียบได้ —
     **ตรงกันทุกตัวอักษร** ทั้ง 4 จุด (ทั่วไป×2 แผน, ผู้ค้ำ, ผู้ปกครอง)
  3. สำหรับ 2 combo ใหม่ที่ไม่เคยมีไฟล์เดิม (วางดาวน์+ผู้ค้ำ, วางดาวน์+ผู้ปกครอง) ตรวจว่ามีเนื้อหาภาคผนวกครบ
     ไม่มี tag หลงเหลือไม่ได้ render และจำนวนรูปภาพที่ต้องอัปโหลดถูกต้อง (2 หรือ 3 ตามกลุ่มลูกค้า)
  **ข้อจำกัดที่ยังไม่ได้ตรวจ (ตรงไปตรงมา):** นี่คือการตรวจ "เนื้อหาข้อความถูกต้องครบถ้วน" เท่านั้น —
  **ยังไม่ได้เปิดดูด้วยตาจริงใน Word ว่าหน้ากระดาษ/กล่องลายเซ็นลอย (floating textbox) จัดวางสวยงามหรือไม่**
  โดยเฉพาะ 2 combo ใหม่ที่เพิ่มเนื้อหาเข้าไปในไฟล์วางดาวน์ (ของเดิมไม่เคยมีส่วนนี้ต่อท้าย) — แนะนำให้เปิด
  `templates/master-downpayment.docx` ด้วย Word สักครั้งก่อนใช้งานจริง เพื่อเช็คหน้าตาสุดท้าย (ยังไม่มี Word
  ในเครื่องนี้ให้เปิดเช็คเอง)
- ที่มา/ขั้นตอนสร้างไฟล์ทั้งหมดอยู่ใน `templates/build_master_templates.js` (รันซ้ำได้ ถ้าไฟล์ต้นฉบับ 3 ไฟล์เปลี่ยน)

## เสร็จเพิ่ม (2026-09-03 รอบ 5)
- **ของแถม**: dropdown อัปเดตให้ตรงกับโปสเตอร์จริง 2 ใบที่ user ส่งมา (ไม่ใช่ลิสต์ Lark เดิม) + แสดงรูปโปสเตอร์
  จริงในขั้นตอน "เลือกของแถม" (`public/assets/gift-downpayment.jpeg`, `gift-installment.jpeg`) กรองตามแผนผ่อน
- **แก้บั๊ก accumulatedAmount**: `api/crm-lookup.js` เดิมกรองเอาแต่ `type==='INSTALLMENT'` ทำให้รายการค่าธรรมเนียม
  (เช่น `CHANGE_INSTALLMENT_TYPE` ที่มี amount ติดลบจริง) หายไปจากยอดสะสม แก้แล้วให้นับทุกรายการที่
  `paymentStatus=SUCCESSFUL && amount≠0` — เพิ่มคอลัมน์ "ประเภท" ในตารางประวัติการชำระด้วย
- **วิธีการผ่อน**: เพิ่มเป็นแถวในตาราง (ไม่ใช่แค่ badge) ทั้งหน้า CS review และฟอร์มลูกค้า ใช้คำว่า
  "เครดิตผ่าน (ผ่อนไปใช้ไป)" / "วางดาวน์" ตรงกันทั้งสองหน้า
- **แก้ input[type=date] แสดงผลกำกวม**: เพิ่ม `dateFieldHtml()` ใน `sign.js` (แพทเทิร์นเดียวกับที่ทำใน
  `cs-review.js` ไปแล้ว) ใช้กับช่อง "วันเดือนปีเกิด" — ยังมี picker ปกติแต่ตัวเลขที่เห็นบังคับเป็น dd/mm/yyyy
  เสมอ ย้าย `isoToDDMMYYYY` ไปไว้ที่ `validation.js` เป็นจุดเดียว (ทั้งสองหน้าเรียกใช้ร่วมกัน)
- **`api/preview-contract.js` (ใหม่)**: สร้าง PDF ตัวอย่างสัญญาจริงให้ลูกค้าอ่านก่อนเซ็น (ปุ่ม "📄 อ่านสัญญา
  ฉบับเต็ม" ในขั้นตอนสุดท้ายของ `sign.js`) — เติมข้อมูลลง master template จริงด้วย docxtemplater แล้ว render
  เป็น PDF เองด้วย pdf-lib (ดูเหตุผลและข้อจำกัดในหัวข้อถัดไป) ต้องกดอ่านก่อนถึงจะเซ็นได้ (`hasViewedContract` gate)
  เพิ่ม `numberToThaiBahtText()` ใน `validation.js` (ตัวเลข -> ตัวอักษรไทย เช่น "สองหมื่นห้าพันเก้าร้อยบาทถ้วน")
  ทดสอบผ่าน dev-server จริงแล้วทั้ง 3 กรณี (ทั่วไป/ผู้ค้ำ/ผู้ปกครอง) + ตรวจสอบว่าชื่อผู้ค้ำ/ผู้ปกครองไหลเข้า
  เนื้อหาจริง + PDF โหลดกลับได้ถูกต้อง (ยังไม่มีเครื่องมือ render หน้า PDF ดูด้วยตาในเซสชันนี้)

## เสร็จเพิ่ม (2026-09-03 รอบ 6, ปรับแก้รอบ 7)
- **เก็บแค่ "อายุ" จากลูกค้า** (ไม่เก็บวันเกิด ไม่เก็บเดือนเกิดด้วย) — ลองเพิ่มช่อง "เดือนเกิด" ไปรอบหนึ่งเพื่อให้
  กฎอนุโลม 3 เดือนก่อนครบ 19 ปียังคำนวณแม่นได้ แต่ user ตัดสินใจ 2026-09-03 ว่าไม่ต้องเพิ่มช่องนี้ เพราะถ้าจะ
  เช็คแม่นจริงควรใช้ **OCR อ่านวันเกิดจากบัตร ปชช. ที่อัปโหลดผ่าน API key แทน** ไม่ใช่ให้ลูกค้าพิมพ์เพิ่มเอง —
  เกณฑ์ตอนนี้จึงเป็นแบบง่าย `อายุ < 19 ปี` เท่านั้น (เอาโค้ด `requiresGuardianFromAgeMonth` ออกแล้ว เพราะไม่ได้ใช้)
  `calcAge`/`requiresGuardian` (เวอร์ชันวันเกิดเต็มเดิม) ยังเก็บไว้ใน `validation.js` เผื่อใช้ตอนทำ OCR จริง
  ในอนาคต — ตอนนั้นกฎอนุโลม 3 เดือนจะกลับมาแม่นยำอีกครั้งด้วยวันเกิดจริงจากบัตร (ดูข้อ 6 ด้านล่าง)

## เสร็จเพิ่ม (2026-09-03 รอบ 8)
- **แก้บั๊ก "กดส่งข้อมูลไม่ได้" หลังเซ็นชื่อ**: root cause คือ `goNext()` เจอ validation error แล้ว `return`
  เฉยๆ ไม่มี error message ให้เห็นเลยในบางเงื่อนไข (ลืมติ๊กยอมรับเงื่อนไข, ไม่ได้แนบลายเซ็นผู้ค้ำ/ผู้ปกครอง) —
  ผู้ใช้เห็นแค่ปุ่มไม่ทำงานโดยไม่รู้สาเหตุ แก้แล้วให้ทุก error แสดงข้อความจริง + เลื่อนจอไปหา error แรกที่เห็น
  ให้อัตโนมัติเมื่อกดปุ่มไม่ผ่าน (กันเคส error message อยู่นอกจอมองไม่เห็น)
- **เพิ่มรูปตัวอย่างประกอบการถ่ายรูปเอกสาร** (บัตร ปชช./รูปคู่บัตร) — เป็นไดอะแกรม SVG วาดเอง (ยังไม่ใช่รูปถ่าย
  จริง เพราะยังไม่มีไฟล์ตัวอย่างจริงให้ใช้) แสดงเหนือช่องอัปโหลดแต่ละช่อง
- **หัวจดหมายจริงบน PDF ตัวอย่างสัญญา**: เพิ่มเมนู "ตั้งค่าหัวจดหมาย" ในหน้า CS review (แพทเทิร์นเดียวกับ
  "อัปโหลด letterhead" ของ debt-tracker) — CS อัปโหลดโลโก้/หัวกระดาษครั้งเดียว ระบบเก็บไว้ (localStorage
  ไปก่อนเพราะยังไม่มี Supabase — ดูข้อ 3) แล้วแปะบนหน้าแรกของ PDF ตัวอย่างสัญญาทุกฉบับที่ generate ให้ลูกค้า
  พบและแก้บั๊กจริงระหว่างทดสอบ: ไฟล์รูปเสีย/เพี้ยนทำให้ pdf-lib **ค้าง** ตอน embedPng (ไม่ใช่แค่ error ปกติ)
  แก้ 2 ชั้น — (1) validate รูปฝั่ง client ด้วย `new Image()` ก่อนเก็บ (2) ใส่ timeout 5 วิ ฝั่ง server กัน
  ค้างถาวรถ้ามีไฟล์เสียหลุดผ่านมาได้ — **เนื้อหาข้อความยังไม่ตรงรูปแบบ Word เป๊ะ 100%** (แค่หัวจดหมายที่ตรงแล้ว
  ส่วน layout ตัวอักษร/ระยะห่างยังเป็นของ pdf-lib เอง ไม่ใช่ layout engine ของ Word จริง)

## เสร็จเพิ่ม (2026-09-03 รอบ 9) — โครงระบบ sidebar + ล็อกอินใหม่ (`app.html`)
- **`public/app.html` + `app.js`** — หน้าเข้าใช้งานหลักใหม่ ให้ตรงกับหน้าตาระบบ CRM จริงที่ user ส่งสกรีนช็อตมา:
  หน้าล็อกอิน (มาสคอต "น้องม่อน 11-01" เด้งเบาๆ ด้วย CSS animation, Username/รหัสผ่าน+ปุ่มโชว์รหัส, ปุ่ม
  "เข้าสู่ระบบ" ทรงแคปซูลไล่สี) ตามด้วย sidebar เมนูซ้าย 7 รายการตามที่ user ระบุ (ข้อมูลลูกค้าทำสัญญา,
  รายงาน, งานสต๊อค, งานแพ็คกิ้ง, Report, อัพโหลดข้อมูล, การตั้งค่า)
- **จำกัดสิทธิ์เมนูตามแผนก**: หน้าล็อกอินมีช่องเลือก "แผนก" (CS/บัญชี/สต๊อค/แพ็คกิ้ง/ผู้จัดการ) — sidebar
  จะกรองให้เห็นเฉพาะเมนูที่แผนกนั้นมีสิทธิ์ (เช่น สต๊อคเห็นแค่ "งานสต๊อค", ผู้จัดการเห็นทุกเมนู) —
  **นี่คือ mock ทั้งหมด** ไม่ได้เช็ค username/password จริงกับฐานข้อมูลพนักงาน (รับทุกค่าที่ไม่ว่างเปล่า)
  ต้องต่อระบบ auth จริงทีหลัง (ข้อ 4 ด้านล่าง)
- **`public/contracts-tab.js` (ใหม่)** — แยกลอจิกทั้งหมดของ "ข้อมูลลูกค้าทำสัญญา" ออกจาก `cs-review.js`
  เดิม เป็นฟังก์ชัน `initContractsTab(containerId)` ให้ทั้ง `cs-review.html` (หน้าเดี่ยว ยังใช้งานได้ปกติ
  เหมือนเดิม) และแท็บ "ข้อมูลลูกค้าทำสัญญา" ใน `app.html` เรียกใช้ร่วมกัน ไม่ต้องเขียนซ้ำ
- **โทนสีทั้งเว็บปรับให้ตรงกับ CRM จริง**: `style.css`'s `--primary` เปลี่ยนจากส้มสดจัด (#ff6a00) เป็น
  ส้ม/พีชอุ่นๆ (#f2914f) ปุ่มหลักเป็นทรงแคปซูลไล่สี ให้ตรงกับปุ่ม "เข้าสู่ระบบ" ในสกรีนช็อต CRM ที่ user ส่งมา
- **เมนูอื่นนอกจาก "ข้อมูลลูกค้าทำสัญญา" ยังเป็นแค่หน้า "🚧 อยู่ระหว่างการพัฒนา"** — user ยังไม่ได้ให้สเปกว่า
  รายงาน/งานสต๊อค/งานแพ็คกิ้ง/Report/อัพโหลดข้อมูล/การตั้งค่า แต่ละอันต้องทำอะไรบ้าง ไม่ได้เดา/สร้างฟีเจอร์เอง

## เสร็จเพิ่ม (2026-09-03 รอบ 10) — ปรับ sidebar ตามฟีดแบ็ก user หลังทดสอบหน้า `app.html`
- **ฟอนต์เว็บทั้งเว็บเปลี่ยนเป็น "Prompt"** — เพิ่ม `@import` Google Fonts ใน `style.css` ตัวเดียว (ทุกหน้าที่โหลด
  `style.css` ได้ฟอนต์ใหม่อัตโนมัติ) fallback เดิม (Noto Sans Thai/Sarabun/system-ui) ยังอยู่ต่อจากนี้เผื่อโหลดไม่ทัน
- **เอาเมนู "รายงาน" ออก** จาก `MENU_ITEMS` ใน `app.js` (ตามที่ user ขอ ไม่ได้ระบุเหตุผล เผื่อรวมเข้ากับ "Report" ทีหลัง)
- **ย้าย "ตั้งค่าหัวจดหมาย" จากแท็บ "ข้อมูลลูกค้าทำสัญญา" ไปอยู่เมนู "อัพโหลดข้อมูล"** — แยกออกมาเป็นไฟล์ใหม่
  `public/letterhead-settings.js` (`initLetterheadSettings(containerId)`) ไม่ปนกับ `contracts-tab.js` อีกต่อไป
  `contracts-tab.js` ยังอ่านค่าจาก localStorage คีย์เดิม (`contractLetterheadDataUrl`) ตอนสร้างลิงก์เหมือนเดิม
  แค่ไม่มี UI อัปโหลดในแท็บนั้นแล้ว **`cs-review.html` (หน้าเดี่ยวเดิม) จึงไม่มีทางตั้งค่าหัวจดหมายได้อีกต่อไป**
  (ต้องเข้าทาง `app.html` → เมนู "อัพโหลดข้อมูล" เท่านั้น) ตั้งใจไม่ผูกซ้ำเพราะ `app.html` เป็นทางเข้าหลักแล้ว
- **เพิ่มเมนูใหม่ "สำหรับ CS"** (key `for_cs`) — ยังเป็นแค่หน้า "🚧 อยู่ระหว่างการพัฒนา" เหมือนเมนูอื่นที่ยังไม่มีสเปก
  (user ยังไม่ได้บอกว่าเมนูนี้ต้องมีอะไรต่างจาก "ข้อมูลลูกค้าทำสัญญา" ที่มีอยู่แล้ว ไม่ได้เดา/สร้างฟีเจอร์เอง)
- **เปลี่ยนชื่อเมนู**: "งานสต๊อค" → "สำหรับสต๊อค", "งานแพ็คกิ้ง" → "สำหรับแพ็คกิ้ง" (ตรงตัวตามที่ user ระบุ)

## เสร็จเพิ่ม (2026-09-03 รอบ 11)
- **ย้ายกล่อง "ค้นหาคำสั่งขาย" (พร้อมเครื่องมือทั้งชุดของ CS — ดึงข้อมูล CRM/ยืนยัน/สร้างลิงก์)
  จากเมนู "ข้อมูลลูกค้าทำสัญญา" ไปอยู่ที่เมนู "สำหรับ CS" แทน** (`app.js` เปลี่ยน tab key ที่เรียก
  `initContractsTab()` จาก `contracts` เป็น `for_cs`) — ตีความว่า "สำหรับ CS" ควรเป็นเครื่องมือหน้างานที่ CS
  ใช้ทำสัญญาจริงทุกวัน ส่วน "ข้อมูลลูกค้าทำสัญญา" เหลือเป็นหน้าว่างรอสเปกใหม่ (เช่น อาจเป็นหน้าดูรายการสัญญา
  ที่สร้างไปแล้ว) **ถ้าตีความผิดไปจากที่ user ต้องการ บอกได้ จะย้ายกลับ/ปรับใหม่ให้**
- **ปรับกล่องค้นหาให้เป็นทรงแคปซูล** ตามภาพตัวอย่างที่ user ส่งมา: ฝั่งซ้ายเป็น `<select>` ("เลขที่สั่งซื้อ SO"
  ตอนนี้มีตัวเลือกเดียว เผื่อเพิ่มวิธีค้นหาอื่นทีหลัง) ฝั่งขวาเป็นไอคอนแว่นขยาย (กดค้นหาได้เลย) + ช่องพิมพ์
  placeholder "พิมพ์เพื่อค้นหา" เอาปุ่ม "ค้นหา" แบบเดิมออก (Enter หรือกดไอคอนแว่นขยายแทน) — CSS ใหม่
  (`.so-search-pill` ใน `style.css`) ใช้ร่วมกับ `contracts-tab.js` (เดิมพึ่ง class `.search-row` ที่จริงๆ
  นิยามไว้แค่ใน `<style>` ของ `cs-review.html` เท่านั้น ทำให้กล่องค้นหาใน `app.html` แสดงผลไม่มีสไตล์มาตลอด
  — แก้จุดนี้ไปด้วยเพราะเป็นสาเหตุที่กล่องค้นหาดูไม่เรียบร้อยในภาพที่ user ส่งมา)
- **ไอคอนเมนูซ้ายทั้งหมดเปลี่ยนจากอิโมจิสีเป็นเส้น outline SVG โทนสีเทาเข้ม** (`--icon-gray: #47505b` ใน
  `style.css`) วาดเอง อิงทรงจาก Feather Icons (ไม่โหลดไลบรารีนอก) — ไอคอนคงสีเทาเข้มเดิมไม่ว่าจะ active/inactive
  (ไม่ได้ขอให้เปลี่ยนสีตอน active จึงไม่แตะ)

## เสร็จเพิ่ม (2026-09-03 รอบ 12)
- **ช่อง "วันเริ่มผ่อนงวดแรก" ในการ์ด "CS กรอกยืนยันก่อนสร้างลิงก์" แก้ไขได้อยู่แล้วจริงๆ** (มี `<input
  type="date">` โปร่งใสซ้อนทับกล่องข้อความอยู่ก่อนแล้ว คลิกที่ไหนในกล่องก็เปิด date picker ได้) แต่ **ไม่มีสัญญาณ
  ภาพใดๆ บอกว่าแก้ไขได้** ทำให้ user เข้าใจว่าเป็นข้อความนิ่งแก้ไม่ได้ — แก้โดยเติมไอคอนปฏิทิน (โทนสีเทาเข้ม
  ชุดเดียวกับไอคอนเมนู) ที่มุมขวาของกล่องผ่าน CSS `.date-display` (`style.css`) แทนที่จะเขียน SVG ซ้ำในแต่ละ
  ไฟล์ JS — มีผลกับทุกช่องวันที่ที่ใช้ class `.date-display`/`.date-field-wrap` ร่วมกันทั้งเว็บ (เช่น "วันเดือนปี
  เกิด" ใน `sign.js` ด้วย ไม่ใช่แค่จุดเดียวที่ user ชี้มา) พร้อมรีแฟกเตอร์ `contracts-tab.js` ให้ใช้ class ร่วม
  แทนการเขียน inline style ซ้ำเอง (เดิมไม่ได้ใช้ pattern เดียวกับ `sign.js`)

## เสร็จเพิ่ม (2026-09-03 รอบ 13) — ปฏิทินเลือกวันที่แบบกำหนดเอง
- **`public/thai-date-picker.js` (ใหม่)** — ปฏิทิน popup กำหนดเองทั้งหมด (เดือน/ปี พ.ศ. ภาษาไทย, วันนี้เป็น
  วงกลมขอบสีฟ้า, วันที่เลือกเป็นวงกลมส้มทึบ, เลื่อนเดือนก่อน/ถัดไปได้) ตามภาพตัวอย่างที่ user ส่งมา แทนที่
  `<input type="date">` เนทีฟที่หน้าตาคุมไม่ได้และแต่ละเบราว์เซอร์/OS แสดงไม่เหมือนกัน ใช้: `attachThaiDatePicker(
  wrapEl, { value, onChange })` กับโครงสร้าง `.date-field-wrap > .date-display` เดิม (ไม่ต้องมี native
  input ซ้อนอยู่ข้างในอีกต่อไป)
- ใช้กับช่อง **"วันเริ่มผ่อนงวดแรก"** ในเมนู "สำหรับ CS" แล้ว (จุดเดียวที่มีช่องวันที่ใช้งานจริงตอนนี้ —
  `dateFieldHtml()` เดิมใน `sign.js` เป็น dead code ไม่ได้ถูกเรียกใช้แล้ว ตั้งแต่ตัด step "วันเดือนปีเกิด" ออก
  ไปตอนรอบ 6 เหลือแค่กรอก "อายุ" เฉยๆ — ยังไม่ได้ลบทิ้ง เผื่อเอากลับมาใช้ตอนทำ OCR วันเกิดจริงในอนาคต)

## เสร็จเพิ่ม (2026-09-03 รอบ 14) — ภาพตัวอย่างการถ่ายรูปเอกสารเป็นการ์ตูน
- **`EXAMPLE_SVG_ID_CARD` / `EXAMPLE_SVG_SELFIE` ใน `sign.js`** วาดใหม่จากไดอะแกรมกล่อง/เส้นสีเทาล้วนเดิม
  (ดูออกยากว่าต้องถ่ายแบบไหน) เป็นภาพการ์ตูนสีสัน: ใบแรกเป็นบัตรประชาชนการ์ตูน (รูปหน้าคนในกรอบรูป + แถบหัวบัตร
  สีฟ้า + บรรทัดข้อมูล) ใบสองเป็นคนการ์ตูนถือบัตรขึ้นข้างใบหน้า ทั้งสองมีกรอบมุมกล้อง 4 มุมล้อมรอบสื่อว่า "จัดให้
  อยู่ในกรอบ" ยังคงข้อความกำกับ "ตัวอย่าง (ภาพประกอบ ไม่ใช่รูปถ่ายจริง)" ไว้เหมือนเดิม กันเข้าใจผิดว่าเป็นรูปอ้างอิง
  จริง — ตรวจสอบด้วยการ render SVG จริงผ่าน headless Chrome ดูภาพแล้วว่าออกมาถูกต้อง ไม่ใช่แค่ syntax check JS

## เสร็จเพิ่ม (2026-09-03 รอบ 15) — ปรับภาพตัวอย่างตามภาพอ้างอิงจริงที่ user ส่งมา
- **รูปตัวอย่าง "บัตรประชาชน"** วาดใหม่ตามภาพ mockup บัตรจริงที่ user ส่งมา (ภาพที่ 4): การ์ดสีฟ้า มีวงกลม
  แดง (แทนตราครุฑ ไม่ได้วาดรายละเอียดจริงเพื่อไม่ลอกเลียนตราสัญลักษณ์ทางการ), เลขบัตร/ชื่อ/วันเกิด/ที่อยู่/
  วันออก-หมดอายุเป็น "X" ทั้งหมด (กันเข้าใจผิดว่าเป็นข้อมูลจริง), รูปในกล่องรูปด้านขวา, บาร์โค้ดซ้าย, ชิปทอง —
  ใช้ `<text>` SVG จริงแทนแท่งสีเทาแบบเดิม
- **รูปตัวอย่าง "คู่บัตรประชาชน"** วาดใหม่ตามภาพที่ user ส่งมา (ภาพที่ 1-3): คนหันหน้าตรงเข้ากล้อง ถือบัตรขึ้น
  ตรงกลางหน้าอก มือ+นิ้วจับขอบล่างบัตรชัดเจน (ไม่ใช่ถือเอียงข้างแก้มแบบรอบก่อน) บัตรในมือเป็น mini version
  ของบัตรใบแรก (วงกลมแดง+กล่องรูป+บรรทัดข้อมูล) ให้เห็นว่าเป็นบัตรใบเดียวกัน
- ตรวจสอบด้วยการ render จริงผ่าน headless Chrome 2 รอบ (รอบแรกเจอว่ามือหายไปเพราะบัตรบังจนหมด แก้โดยวาด
  นิ้ว/หัวแม่มือทับขอบบัตรทีหลังแทน)

## เสร็จเพิ่ม (2026-09-03 รอบ 16) — ตัวอย่างสัญญาสอดคล้องกับเทมเพลตจริงมากขึ้น + เลขที่สัญญาใหม่
- **พบและแก้บั๊กจริงในเทมเพลตต้นฉบับ**: `templates/master-installment.docx` งวดที่ 9 ในตารางผ่อนใช้ tag
  `{ยอดผ่อน_10}` ผิด (ควรเป็น `{ยอดผ่อน_9}`) ทำให้ยอดผ่อนงวด 9 ซ้ำกับงวด 10 ทุกสัญญาที่มีตั้งแต่ 9 งวดขึ้นไป —
  ตรวจสอบแล้วบั๊กนี้มาจากไฟล์ต้นฉบับจริง "สัญญาเช่าซื้อ+หนังสือค้ำประกัน (Revise 26.05.2026).docx" เอง (ไม่ใช่
  เกิดจากการรวมไฟล์ของเรา) ไฟล์ต้นฉบับอีก 5 ไฟล์ไม่มีบั๊กนี้ — แก้ที่ `master-installment.docx` แล้วด้วยการ
  แก้ไข XML ตรงตำแหน่ง (ไม่กระทบเนื้อหาอื่น) รัน `verify_masters.js` ซ้ำผ่านหมดเหมือนเดิม — **แนะนำแจ้งทีมที่
  ดูแลไฟล์ Word ต้นฉบับให้ทราบด้วย เผื่อมีที่ใช้ไฟล์นั้นตรงๆ นอกระบบนี้**
- **`api/_lib/docx-blocks.js` (ใหม่)**: แยกเนื้อหา document.xml (หลัง docxtemplater เติมข้อมูล) เป็น block
  ย่อหน้า/ตารางเรียงตามที่ปรากฏจริง แทนที่จะตัด tag ทิ้งเหลือข้อความก้อนเดียว — ตารางแสดงการผ่อนชำระ (3
  คอลัมน์ 12 แถว) ในเทมเพลตจริงตอนนี้แยกออกมาเป็นตารางแล้ว ไม่ปนกับข้อความ
- **`api/_lib/pdf-text-layout.js`**: เพิ่ม `drawTable()` วาดตารางเป็นกริดจริงด้วย pdf-lib (เส้นขอบ, แถวหัวตาราง
  พื้นเทา, ขึ้นหน้าใหม่กลางตารางได้ถ้ายาวเกินหน้า) — `renderTextToPdf()` รับ `opts.blocks` (ย่อหน้า/ตาราง
  เรียงลำดับจริง) แทน `opts.body` string ก้อนเดียวแบบเดิม (ยังรับ `opts.body` แบบเก่าได้เผื่อโค้ดอื่นเรียก)
- **เลขที่สัญญารูปแบบใหม่ `SALMONyyyymmdd-xxxxx`** (2026-09-03 user ระบุ) — `buildContractNo()` ใน
  `validation.js`, `xxxxx` คือเลข 5 หลักท้ายของเลข SO เดิม (รูปแบบเลข SO จริงคือ `SO-YYYYMMDDNNNNN` มี 5 หลัก
  ท้ายไม่ซ้ำกันอยู่แล้ว การันตี unique ได้โดยไม่ต้องมีตัวนับส่วนกลางเพิ่ม เพราะระบบนี้ยังไม่มี DB จริง) ผูกเข้า
  `session.contractNo` ตอน CS กด "สร้างลิงก์" ใน `contracts-tab.js` แล้ว, `mock-session.js` อัปเดตให้ใช้รูปแบบ
  ใหม่ด้วย (เดิมเป็น `CT-...`)
- **ยังไม่ตรงกับ Word แบบ pixel-perfect** — นี่คือการปรับปรุงให้ "สอดคล้องกับสัญญา" มากขึ้น (เนื้อหา+โครงสร้าง
  ตารางถูกต้อง) ไม่ใช่การแปลง docx→PDF จริงแบบ WYSIWYG ยังต้องรอ LibreOffice/CloudConvert (ยังไม่มีในเครื่องนี้
  ตรวจสอบแล้วไม่มี LibreOffice ติดตั้งอยู่) ถ้าต้องการ pixel-perfect เป๊ะต้องตัดสินใจเรื่องนี้ก่อน (ดูข้อ 1
  ด้านล่าง)
- ตรวจสอบ: เติมข้อมูลตารางผ่อนด้วยยอดที่ไม่ซ้ำกันทีละแถวจริงผ่าน docxtemplater แล้ว parse กลับมาเช็คว่าทั้ง 12
  แถวขึ้นค่าถูกต้องไม่ซ้ำกัน (ยืนยันบั๊กงวด 9 หายแล้วจริง) + เรียก `api/preview-contract.js` จริงสร้าง PDF
  สำเร็จทั้ง 3 เคส (ผ่อนธรรมดา 12 งวด, วางดาวน์+ผู้ค้ำ, ผ่อน+ผู้ปกครอง) ไม่มี error — **ยังไม่ได้เปิดดู PDF
  ด้วยตาจริงในเซสชันนี้** (ลอง render ผ่าน pdf.js ใน headless Chrome แต่ worker ไม่ยอมโหลดใน headless mode
  ลองแล้วหลายวิธี ไม่คุ้มเวลาที่จะแก้ต่อ) แนะนำเปิด `sign.html` จริงในเบราว์เซอร์กด "อ่านสัญญาฉบับเต็ม" เพื่อดู
  ตารางที่ได้ด้วยตาก่อนใช้งานจริง

- **เพิ่มปุ่ม "📄 ดูตัวอย่างสัญญา (PDF)" ในเมนู "สำหรับ CS" หลังกด "สร้างลิงก์"** — เรียก
  `/api/preview-contract` ตรงด้วยข้อมูล session จริงจาก CRM (ราคา/ตารางผ่อนจริง) + placeholder "-" แทน
  ข้อมูลลูกค้า (ยังไม่มีให้ตอนนี้เพราะลูกค้ายังไม่กรอกฟอร์ม) เปิดในแท็บใหม่ทันที ไม่ต้องเดินฟอร์มลูกค้าทั้งชุด
  ก่อนถึงจะเห็นเอกสารจริง (2026-09-03 user ขอ "ในหน้าเว็บทดสอบต้องการเห็นตัวอย่างเอกสารจริงทั้งหมด")

## เสร็จเพิ่ม (2026-09-03 รอบ 17) — แก้บั๊ก dev-server hot-reload ทำให้ตัวอย่างสัญญาว่างเปล่า
- **root cause ที่ user เจอ "แบบฟอร์มสัญญาไม่มีข้อมูลเลย"**: ไม่ใช่บั๊กที่ logic การ render เลย (ตรวจสอบด้วย
  การเรียก handler ตรงในสคริปต์แยกแล้วได้ข้อความ/ตารางครบถูกต้อง) แต่เป็นบั๊กที่ `dev-server.js` เอง — เดิม
  `serveApi()` ลบ require cache แค่ไฟล์ `api/<name>.js` ตัวเดียวทุก request (comment บอกว่า "hot-reload ทุก
  request") แต่ไฟล์ที่มันไป `require()` ต่อ (เช่น `api/_lib/pdf-text-layout.js`, `api/_lib/docx-blocks.js`)
  ไม่ได้ถูกลบ cache ด้วย — เพราะ dev-server process รันค้างมาตั้งแต่ก่อนหน้ารอบนี้ (เจอว่ามีรันอยู่แล้วตั้งแต่
  ต้นเซสชัน) โค้ดเก่าของ `pdf-text-layout.js` (ที่ยังรับ `opts.body` string ไม่รู้จัก `opts.blocks`) เลยค้างอยู่
  ใน memory แม้ไฟล์บนดิสก์จะแก้ไปแล้ว — `preview-contract.js` (ที่ reload ปกติ) ส่ง `blocks` มาให้ แต่ฟังก์ชัน
  เก่าไม่รู้จักคีย์นี้ มองว่า `opts.body` เป็น `undefined` เลยวาดแค่หัวเรื่องแล้วจบ (เนื้อหาว่างเปล่าตามที่เห็น)
- **แก้แล้ว**: `serveApi()` เปลี่ยนเป็นลบ require cache ทุกไฟล์ในโปรเจกต์ (ไม่แตะ `node_modules`) ก่อนทุก
  request แทนที่จะลบแค่ไฟล์เดียว กัน bug แบบนี้เกิดซ้ำกับไฟล์ `_lib/*.js` อื่นในอนาคตด้วย
- **รีสตาร์ท dev-server แล้ว** (จำเป็นเพราะแก้ dev-server.js เอง — ไฟล์ตัวมันเองไม่ได้ hot-reload ตัวเอง) +
  ยิง request จริงผ่าน HTTP (ไม่ใช่แค่เรียก handler ตรงในสคริปต์) ยืนยันแล้วว่าได้เนื้อหาครบ (6 หน้า, มี text
  draw operator 36 จุดแค่หน้าแรก) **user ควรลองเปิด/สร้างลิงก์ใหม่อีกครั้งในเบราว์เซอร์ ของเดิมที่ค้างอยู่ (ถ้ามี
  tab เปิดค้าง) อาจยังเป็น blob เก่าจากก่อนแก้ ต้องเรียกใหม่**

## เสร็จเพิ่ม (2026-09-04 รอบ 18) — ขึ้นระบบจริง: repo ใหม่ + Vercel + Supabase project จริง
- **สร้าง repo ใหม่ `tikkysalmon/contract-system`** (public, ยืนยันกับ user แล้ว) ที่ `C:\Users\user\contract-
  system` ตามแพทเทิร์นเดิมของโปรเจกต์ (แยก repo นอก `AI\claude code` เหมือน `debt-tracker`/`esign-approval`)
  copy `public/`, `api/`, `templates/`, `supabase-setup.sql`, `package.json` ฯลฯ มาจากโฟลเดอร์นี้ (ไม่รวม
  `node_modules/`) รัน `npm install` + `templates/verify_masters.js` ผ่านหมดในที่ใหม่ก่อน push จริง
- **user สร้าง Supabase project ใหม่เองแล้ว** (แยกจาก debt-tracker ตามที่ user ขอ เพราะข้อมูลใหญ่) รัน
  `supabase-setup.sql` ผ่าน SQL Editor เรียบร้อย — ตรวจสอบแล้วว่าตาราง `staff_users`/`contract_sessions`/
  `contract_submissions` ถูกสร้างจริงผ่าน REST API
- **`api/create-session.js` + `api/get-session.js` (ใหม่)**: แทนที่ localStorage demo bridge เดิมด้วย
  Supabase จริง — CS กด "สร้างลิงก์" เขียน row ใหม่ลง `contract_sessions` จริง (เก็บ session object เต็มไว้ใน
  `crm_snapshot` jsonb) ได้ token จริงกลับมา ลูกค้าเปิด `sign.html?token=...` ดึงข้อมูลจริงจาก Supabase — **ใช้
  งานได้ข้ามเครื่อง/เบราว์เซอร์แล้ว** (ไม่ใช่แค่ localStorage ในเบราว์เซอร์เดียวแบบเดิม)
  - **เจอบั๊กจริงระหว่างทดสอบ**: ตาราง `contract_sessions` เปิด RLS อยู่ (ไม่ใช่ปิดตามที่คาดตอนแรก) ใช้
    `SUPABASE_ANON_KEY` เขียนไม่ได้ (error 42501) — แก้โดยเปลี่ยนไปใช้ **`SUPABASE_SERVICE_ROLE_KEY`** แทน
    (ปลอดภัยเพราะโค้ดนี้รันฝั่ง server เท่านั้น ไม่เคยถูกเรียกตรงจากเบราว์เซอร์) ต้องตั้งค่านี้ใน Vercel
    project settings แทน `SUPABASE_ANON_KEY` — **ยังไม่ต้องตั้งค่า RLS policy เพิ่มเติม**
  - ทดสอบ round-trip จริงผ่าน HTTP ครบ (create-session → get-session) ได้ข้อมูลถูกต้อง 100% รวมถึงข้อความ
    ภาษาไทย (เจอปัญหา encoding เพี้ยนตอนทดสอบครั้งแรกแต่เป็นแค่ปัญหา encoding ของ terminal ตอนพิมพ์ curl เอง
    ไม่ใช่บั๊กจริงในโค้ด — ยืนยันด้วยการส่ง payload ผ่านไฟล์ JSON แทน ได้ผลถูกต้อง) แล้วลบ test row ทิ้งจาก
    ฐานข้อมูลจริงเรียบร้อย
- **`contracts-tab.js`**: `createLink()` เป็น async แล้ว เรียก `/api/create-session` จริง มีสถานะ loading/error
  ระหว่างสร้างลิงก์ การ์ด "สร้างลิงก์แล้ว" เปลี่ยนจากปุ่มเปิดโหมดทดสอบ (`?demo=1`) เป็นกล่องแสดง URL จริง
  พร้อมปุ่ม "คัดลอก" ให้ CS เอาไปส่งลูกค้าทาง LINE/SMS ได้เลย
- **`sign.js`**: อ่าน `?token=...` จาก URL ก่อนเป็นอันดับแรก (ถ้ามี ดึงจริงจาก `/api/get-session`) ตกไปที่
  `?demo=1`/localStorage แล้วค่อยตกไปที่ `MOCK_SESSION` ตามลำดับเดิมถ้าไม่มี token จริง — ถ้าดึง token จริง
  ไม่สำเร็จ (ลิงก์ผิด/หมดอายุ) จะโชว์ข้อความ error แทนที่จะ render ฟอร์มเปล่าๆ
- **ยังไม่ทำในรอบนี้**: ขั้นตอน "ลูกค้ากรอกฟอร์มเสร็จ → ส่งกลับ" (`submitContract()`) ยังเป็น mock (แค่ log
  ไม่ได้เขียนลง `contract_submissions` หรืออัปโหลดรูปเข้า Supabase Storage จริง) — ต้องทำต่อเป็นรอบถัดไป ก่อน
  จะให้ลูกค้าจริงใช้งานได้ครบวงจร

## เสร็จเพิ่ม (2026-09-04 รอบ 19) — ขั้นตอนลูกค้าส่งฟอร์มเสร็จเขียนลง Supabase จริงแล้ว
- **`api/submit-contract.js` (ใหม่)**: ลูกค้ากด "ส่งข้อมูล" หลังเซ็นชื่อ (ขั้นตอนสุดท้ายของ `sign.js`) ตอนนี้
  (1) อัปโหลดรูปเอกสาร/ลายเซ็นทั้งหมด (บัตร ปชช./เซลฟี่/ผู้ค้ำ/ผู้ปกครอง/ลายเซ็น 3 แบบ) เข้า Supabase Storage
  bucket `contract-files` จริง (2) insert แถวใหม่ลง `contract_submissions` (เก็บ `customer_data` แยกจากไฟล์ —
  ไฟล์อยู่ใน Storage อ้างด้วย `file_paths`) (3) อัปเดต `contract_sessions.status` เป็น `'submitted'` — แทนที่
  `console.log` mock เดิมทั้งหมด
- **`sign.js`'s `submitContract()`**: เรียก `/api/submit-contract` จริงเมื่อเปิดผ่าน token จริง (`realToken`)
  มีสถานะ loading/error ระหว่างส่ง (ปุ่ม "ส่งข้อมูล" disable + ข้อความ error inline ถ้าพัง) — ถ้าเปิดแบบ
  `?demo=1`/`MOCK_SESSION` ตรงๆ (ไม่มี token จริง) ยังใช้ mock เดิม (แค่ log) เหมือนก่อน เพราะไม่มี session
  จริงใน DB ให้ผูก
- **ทดสอบจริงครบวงจรตามที่ user ขอ ("ใช้ข้อมูลที่เป็น mock ก่อนบนระบบจริง")**: สร้าง session จริง → submit
  ด้วยข้อมูล/รูปภาพ mock (รูป PNG 1x1 ปลอม) → ตรวจสอบว่า Storage มีไฟล์จริง (download กลับมาได้), 
  `contract_submissions` มีแถวถูกต้อง (ข้อความไทยถูกต้อง, ไม่มี base64 รูปปนอยู่), `contract_sessions.status`
  เปลี่ยนเป็น `submitted` จริง → **ลบข้อมูลทดสอบทั้งหมดออกแล้วตามที่ user ขอ** (แถว submissions, ไฟล์ 3 ไฟล์ใน
  Storage, แถว sessions) ตรวจสอบซ้ำว่าทั้ง 3 จุด (`contract_sessions`, `contract_submissions`, Storage) ว่าง
  เปล่าสนิทแล้วหลังลบ
- **ข้อจำกัดที่ทราบแล้ว (ยังไม่แก้รอบนี้)**: ไฟล์ทั้งหมดส่งเป็น base64 ปนกับข้อมูลลูกค้าใน JSON ก้อนเดียวไปที่
  `/api/submit-contract` — ถ้าลูกค้าอัปโหลดรูปจริงหลายไฟล์ขนาดใหญ่ (ไม่ใช่รูปทดสอบเล็กๆ แบบนี้) รวมกันอาจชน
  ขีดจำกัดขนาด request body ของ Vercel serverless function ได้ วิธีที่ถูกต้องกว่าสำหรับใช้งานจริงคือให้ browser
  อัปโหลดตรงเข้า Supabase Storage ผ่าน signed URL แทน ยังไม่ได้ทำ — ควรทดสอบด้วยรูปขนาดจริง (2-5MB) ก่อน
  ประกาศว่าใช้งานได้จริงกับลูกค้า

## เสร็จเพิ่ม (2026-09-04 รอบ 20) — รวมหลาย SO เข้าลิงก์เดียวกัน (วางดาวน์เครื่อง + อุปกรณ์เสริม)
- **ข้อจำกัดของ CRM ที่ user แจ้ง**: ซื้อวางดาวน์เครื่อง + อุปกรณ์เสริมพร้อมกัน CRM บังคับเปิดแยกเป็นคนละ SO
  แต่ต้องการให้ลูกค้ากรอกฟอร์ม/เซ็นชื่อ**ครั้งเดียว** — ตกลงดีไซน์กับ user แล้ว: กรอก/เซ็นครั้งเดียว แต่ระบบ
  ออก**สัญญาแยกฉบับต่อ SO** (แต่ละฉบับมีตารางผ่อนของตัวเอง ไม่รวมยอดข้ามรายการ)
- **`api/crm-lookup.js` รีแฟกเตอร์**: แยก `buildSoData()` ใช้ร่วมกันได้ทั้ง SO หลักและ SO อื่น ตอนนี้เรียก
  `GET /crm/customer/{id}` เพิ่มเพื่อดึง SO อื่นของลูกค้าคนเดียวกัน แล้ว resolve แต่ละ SO ให้ครบ (ชื่อสินค้า/ราคา/
  ตารางผ่อน) คืนเป็น `otherItems[]` ในผลลัพธ์ — ทดสอบกับตัวอย่างจริงจาก user (SO-2026090200106 iPad + 
  SO-2026090200108 Apple Pencil ของลูกค้าคนเดียวกัน) ได้ข้อมูลถูกต้องครบ
- **`public/contracts-tab.js`**: หลัง CS ค้นหา SO หลัก ถ้ามี SO อื่นของลูกค้าคนเดียวกัน จะโชว์การ์ด "พบคำสั่งขาย
  อื่นของลูกค้าคนนี้" ให้ติ๊กเลือกรวมเข้าลิงก์เดียวกัน แต่ละ SO ที่เลือก (รวม SO หลัก) มีการ์ด "CS กรอกยืนยัน"
  แยกของตัวเอง (จำนวนงวด/วันเริ่มผ่อน) ตามที่ user ยืนยันว่าต้องแยกตารางผ่อนตาม SO เดิม — "สร้างลิงก์" สร้าง
  session เดียวที่มี `items[]` แทนที่ shape แบบ 1 SO/session เดิม
- **`public/sign.js` ปรับทุกขั้นตอนให้วนลูป `session.items[]`**: รายการที่ทำสัญญา (โชว์ทุกรายการ), ตารางผ่อนชำระ
  (แยกตารางต่อ SO), ขั้นตอนเซ็นชื่อ (ปุ่ม "อ่านสัญญาฉบับเต็ม" แยกทีละ SO ต้องกดอ่านครบทุกฉบับก่อนถึงจะเซ็นได้ —
  เดิมเป็น `hasViewedContract` boolean เดียว เปลี่ยนเป็น `viewedContracts` เก็บสถานะแยกต่อ SO)
- **`api/preview-contract.js` และ `api/submit-contract.js` ไม่ต้องแก้เลย** — ยังรับ "session แบบเดี่ยว" (1
  รายการ) เหมือนเดิม ฝั่ง client (contracts-tab.js/sign.js) แค่ประกอบ flat session จาก `item` + ฟิลด์ร่วม
  (`contractDate`/`customer`/`letterheadDataUrl`) แล้วเรียกทีละ SO แทน — ลดจุดที่ต้องแก้ลงมาก
- **ทดสอบจริงครบวงจรบนระบบจริงด้วยตัวอย่างจริงของ user**: ค้นหา SO หลัก → เจอ SO อื่นถูกต้อง → สร้าง session
  2 รายการจริงผ่าน `/api/create-session` → `/api/get-session` คืนข้อมูลครบ 2 รายการถูกต้อง → `sign.html?token=`
  โหลดได้ → เรียก `/api/preview-contract` แยกทีละ SO สำเร็จทั้งคู่ (ได้ PDF จริง 2 ฉบับคนละสัญญา) → **ลบ session
  ทดสอบทิ้งแล้ว** (เจอ session จริงอีกแถวหนึ่งที่ user เคยทดสอบเองผ่าน CS UI ระหว่างตรวจ ไม่ใช่ของทดสอบรอบนี้
  — **ไม่ได้ลบ** เพราะไม่ใช่ข้อมูลที่ผมสร้างเอง)
- **ยังไม่ได้ทำ**: การส่งฟอร์มจริงตอนจบ (`submit-contract.js`) ยังบันทึกข้อมูลลูกค้า/ไฟล์แค่ชุดเดียวต่อ session
  ไม่ได้ผูกแยกต่อ SO อย่างชัดเจน (ใช้ได้เพราะข้อมูลลูกค้า/ไฟล์เป็นชุดเดียวกันจริงตามสเปก) — แต่ **ยังไม่มีการ
  สร้างสัญญาจริง (final PDF) แยกฉบับต่อ SO ให้ดาวน์โหลด/จัดเก็บถาวรหลังลูกค้าส่งฟอร์ม** (contract-generate.js
  ยังไม่มีอยู่ดี ตามที่บันทึกไว้ในข้อ 1 ด้านล่างอยู่แล้ว) เมื่อสร้างจริงต้องวนสร้างทีละ SO ด้วยเช่นกัน

## เสร็จเพิ่ม (2026-09-04 รอบ 21) — ค้นหาด้วยชื่อลูกค้า + ตารางเลือก SO แบบเดียวกับ CRM จริง
- **หา endpoint ค้นหาลูกค้าด้วยชื่อจริงสำเร็จแล้ว** — เดา query param เองไปหลายแบบ (`?name=`, `?search=`,
  `?keyword=` ฯลฯ บน `/crm/sale-order`) ไม่มีตัวไหนกรองได้จริง (endpoint เพิกเฉยต่อ param ที่ไม่รู้จักแล้วคืน
  ลิสต์ default เดิมเสมอ ทำให้ดูเหมือนได้ผลลัพธ์แต่จริงๆ ไม่ได้กรอง) — user ช่วยเปิด Network tab ของหน้า CRM
  จริงแล้วส่ง Request URL มาให้ ถึงเจอตัวจริง: `GET /crm/customer?page=1&pageSize=10&mode=quick&searchBy=
  name&searchValue=<ชื่อ>` คืน `{customers: [...], summary, pagination}`
- **`api/crm-lookup.js` รองรับ 3 โหมด query แล้ว**: `?so=` (เดิม), `?name=` (ค้นหาชื่อ — ถ้าเจอเกิน 1 คนคืน
  ลิสต์ให้เลือกก่อน, เจอพอดี 1 คนดึงลิสต์ SO ให้เลย), `?customerId=` (ดึง SO ของลูกค้าที่เลือกจาก step ก่อน)
  — โหมดค้นหาชื่อคืน "ลิสต์เบา" (สถานะ/เครดิต%/สถานะชำระ/เลท/วันที่สร้าง — ตรงกับตารางในหน้า CRM จริงเป๊ะ
  ตามภาพตัวอย่างที่ user ส่งมา) ไม่ resolve ราคา/ตารางผ่อนเต็มของทุก SO ล่วงหน้า (ลูกค้าอาจมีประวัติซื้อเก่า
  เยอะที่ไม่เกี่ยวข้อง) — resolve เต็มเฉพาะ SO ที่ CS ติ๊กเลือกจริงทีหลัง ผ่าน `?so=` เดิม
- **`public/contracts-tab.js`**: dropdown ค้นหาเพิ่มตัวเลือก "ชื่อลูกค้า" คู่กับ "เลขที่สั่งซื้อ SO" เดิม —
  ค้นหาด้วยชื่อแล้วโชว์ตาราง SO ทั้งหมดของลูกค้า (คอลัมน์ตรงกับภาพตัวอย่างจาก CRM ที่ user ส่งมา) ให้ติ๊กเลือก
  กด "ดำเนินการต่อ" แล้วเข้าสู่ขั้นตอน "CS กรอกยืนยันก่อนสร้างลิงก์" แบบเดิม (แยกการ์ดยืนยันจำนวนงวด/วันเริ่ม
  ผ่อนต่อ SO เพราะระยะเวลาผ่อน/ยอดผ่อนอาจไม่เท่ากันตามที่ user ระบุ) แล้วสร้างลิงก์เดียวรวมทุก SO ที่เลือกได้
  เหมือนเดิมทุกประการ (ใช้ตรรกะ/state เดียวกับโหมดค้นหาด้วย SO ทั้งหมด ไม่ต้องแก้ preview/submit/create-session)
- **ทดสอบ backend ทุก endpoint ผ่าน production จริงแล้ว**: ค้นหาชื่อเจอ 1 คนตรงถูกต้อง (หทัยรัตน์ อินยาศรี,
  เครดิต 7.99%/9.48% ตรงกับภาพที่ user ส่งมาเป๊ะ), ค้นหาชื่อกว้างๆ ได้ลิสต์หลายคนถูกต้อง (ทดสอบ pageSize=20 —
  **ข้อจำกัดที่ทราบแล้ว**: ถ้าชื่อที่ค้นตรงกับลูกค้ามากกว่า 20 คน จะเห็นแค่ 20 คนแรก ยังไม่ทำ pagination/
  "โหลดเพิ่ม" ให้) — **ยังไม่ได้ click-test จริงในเบราว์เซอร์** (ตรวจโค้ด/state logic ละเอียดแล้วแต่ไม่มี
  เครื่องมือ automate browser ในเซสชันนี้) รบกวน user ทดสอบคลิกจริงในหน้า "สำหรับ CS" ก่อนใช้งานจริง

## ยังไม่ได้ทำ / บล็อกอยู่
1. **`api/preview-contract.js` ยังไม่ใช่ PDF ตัวจริงที่จะส่งมอบท้ายสุด** — เป็น "ตัวอย่างให้อ่านก่อนเซ็น" เท่านั้น
   (layout จาก pdf-lib เอง ไม่ตรงกับหน้าตา Word ต้นฉบับเป๊ะ, ยังไม่ฝังรูปถ่ายบัตร/เซลฟี่/ลายเซ็นจริงในตัวอย่าง —
   แสดงเป็น "[แนบไฟล์รูปถ่าย]" แทน) ส่วนไฟล์ **สัญญาจริงที่ส่งมอบหลังเซ็น** (`api/contract-generate.js`) ยังไม่ได้
   เขียน ต้องแก้ปัญหา `{%src}` image tag กับ `docxtemplater-image-module-free` ก่อน (ทดสอบแล้ว error) และตัดสินใจ
   ว่าจะแปลง docx→PDF จริงด้วยอะไร (CloudConvert หรืออื่น) เพื่อให้ได้ layout ตรงกับต้นฉบับ Word เป๊ะ
2. **แนะนำเปิด `master-downpayment.docx` เช็คด้วย Word สักครั้ง** ก่อนใช้งานจริง (ยังไม่มี Word ในเครื่องนี้)
3. **Supabase project จริง** ยังไม่ได้สร้าง (แค่เขียน schema SQL รอไว้) — "สร้างลิงก์" ตอนนี้ใช้ localStorage
   เป็นสะพานทดสอบในเครื่องเท่านั้น (ดู `cs-review.js`/`sign.js` โค้ดคอมเมนต์ TODO) ต้องเปลี่ยนเป็น token จริงจาก DB
4. **auth staff จริง** — `app.html` มีหน้าล็อกอิน+จำกัดเมนูตามแผนกแล้ว แต่เป็น mock ล้วน (เช็คกับลิสต์
   `MOCK_USERS` ในโค้ด `app.js` ตรงๆ ไม่ใช่ฐานข้อมูลจริง) **แผนกผูกกับผู้ใช้งานแล้ว ไม่ให้เลือกเองตอนล็อกอิน**
   (แก้ตามที่ user ขอ 2026-09-03) — user/password ทดสอบ (5 แผนก):
   | Username | Password | แผนก |
   |---|---|---|
   | `cs1` | `1234` | CS |
   | `acc1` | `1234` | บัญชี |
   | `stock1` | `1234` | สต๊อค |
   | `pack1` | `1234` | แพ็คกิ้ง |
   | `admin` | `1234` | ผู้จัดการ (เห็นทุกเมนู) |

   **การกรองเมนูตามแผนกปิดไว้ชั่วคราว** (`SHOW_ALL_MENUS = true` ใน `app.js`) — user ขอ 2026-09-03 ให้ทุก
   บัญชีเห็นครบทุกเมนูไปก่อนจนกว่าจะตั้งค่า User ใช้จริง โครงสร้างสิทธิ์ตามแผนก (`departments` ใน
   `MENU_ITEMS`) ยังเก็บไว้ครบ แค่สลับ `SHOW_ALL_MENUS` เป็น `false` ก็เปิดการกรองกลับมาได้ทันที

   ต้องต่อระบบ auth จริง (เหมือน `12_esign-approval`'s `auth.js`) พร้อมตาราง `staff_users` ที่มีคอลัมน์แผนก
   จริงจาก Supabase ทีหลัง — ตอนนั้นค่อยย้าย "ตั้งค่าแผนกของผู้ใช้งาน" ไปอยู่ในเมนู Admin Management/การตั้งค่า
   จริง — `cs-review.html` (หน้าเดี่ยวเดิม) ก็ยังไม่มีการล็อกอินป้องกันเช่นกัน
5. **บุคคลอ้างอิง (ชื่อบุคคลที่ติดต่อได้คนที่หนึ่ง)** — เทมเพลตสัญญาต้องใช้ฟิลด์นี้แต่ฟอร์มลูกค้ายังไม่มีขั้นตอนเก็บ
   ข้อมูลนี้เลย (ตอนนี้ `preview-contract.js` ปล่อยว่างไว้) ต้องเพิ่ม step ใหม่
6. **OCR/ระบบตรวจสอบข้อมูลอัตโนมัติ** — user ส่งตัวอย่างจริงมาแล้ว (บอท Lark ตรวจชื่อ/เลขบัตรตรงกับระบบ,
   หน้าคู่บัตรตรงกับบัตร, อายุในระบบ vs อายุจากบัตรต่างกันเกิน ±1 ปี, เบอร์ซ้ำในตาราง ฯลฯ) — เป็นการตรวจสอบ
   แบบ AI ที่ซับซ้อนกว่า OCR ธรรมดามาก (OCR + face-match + reasoning) — ยืนยันแล้วว่าไม่ได้ใช้ Lark MCP เจาะจง
   ค่ายไหน ระบบใหม่ควรเรียก Claude API ตรงจาก backend เอง (ประเมินราคาไว้แล้ว ~0.3-0.6 บาท/ครั้ง) ยังไม่ได้
   เริ่มออกแบบ/ขอ ANTHROPIC_API_KEY
